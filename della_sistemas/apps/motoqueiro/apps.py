from django.apps import AppConfig


class MotoqueiroConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.motoqueiro"
    label = "motoqueiro"
    verbose_name = "Motoqueiro (Lalamove / Loggi)"
